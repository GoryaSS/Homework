//========== VAR 1 - alert ==========

let leapYear = prompt('Enter the year and see if it is the Leap year', 1996);
alert(leapYear % 4 == 0 || leapYear % 400 == 0 ? 'TRUE' : 'FALSE');

//========== VAR 2 - console.log ==========
// let leapYear = (1996);
// console.log(leapYear % 4 == 0 || leapYear % 400 == 0 ? 'TRUE' : 'FALSE');